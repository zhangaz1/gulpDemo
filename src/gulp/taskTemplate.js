'use strict';

module.exports = taskFactory;

function taskFactory(context) {
    var gulp = context.gulp;

    return taskHandler;

    // return;

    function taskHandler() {
        // do something here...
    }
}